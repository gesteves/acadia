<% unless data.lastfm.weeklyartistchart.artist.nil? %>
  <section class="b-card">
    <ol>
      <% data.lastfm.weeklyartistchart.artist.each do |artist| %>
        <li><%= link_to artist.name, artist.url %></li>
      <% end %>
    </ol>
  </section>
<% end %>