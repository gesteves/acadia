<% unless data.links.response.posts.empty? %>
  <section class="m-card m-writing">
    <h1>Writing</h1>
    <ol>
      <% data.links.response.posts.each do |link| %>
        <li>
          <h2 class="m-writing__headline"><%= link_to link.title, link.url %></h2>
          <%= link.description %>
        </li>
      <% end %>
    </ol>
  </section>
<% end %>
